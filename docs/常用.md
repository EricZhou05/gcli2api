### 打包 Docker 镜像

```bash
docker build -t ericzhou05/gcli2api:latest .
```

### 导出 Docker 镜像
```bash
docker save -o gcli2api_latest.tar ericzhou05/gcli2api:latest
```

### 上传NAS
自动网页上传失败
手动ssh进入root模式
然后
```bash
cat /volume1/docker/gcli2api_latest.tar | docker load
```